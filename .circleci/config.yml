version: 2

jobs:
  deploy-lint:
    docker:
      - image: alpine:latest
    steps:
      - run:
          name: foobar
  build:
    docker:
      - image: alpine:latest
    steps:
      - run: echo built
  deploy:
    docker:
      - image: alpine:latest
    steps:
      - run: echo deployed

workflows:
  version: 2
  deploy-production:
    jobs:
      - deploy-lint
      - build
      - deploy:
          requires:
            - build

