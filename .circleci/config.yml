version: 2

jobs:
  deploy-lint:
    run:
      name: foobar
  build:
    docker:
      - image: alpine:latest
    steps:
      - run: echo built
  deploy:
    docker:
      - image: alpine:latest
    steps:
      - run: echo deployed

workflows:
  version: 2
  deploy-production:
    jobs:
      - build
      - deploy:
          requires:
            - build

