version: 2

jobs:
  build:
    docker:
      - image: ubuntu
        command: []
    steps:
      - name: foo
        command: echo hello

workflows:
  version: 2
  test:
    jobs:
      - build:
          filters:
            tags:
              only: /^foo.+/
