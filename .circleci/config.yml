version: 2
jobs:
  build:
    docker:
      - ubuntu:latest
    steps:
      - checkout
      - echo "foo!"

workflows:
  version: 2
  workflow:
    jobs:
      - build:
          pre-steps: "echo 'before checkout'"
