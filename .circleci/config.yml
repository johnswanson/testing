version: 2

jobs:
  build:
    docker:
      - image: ubuntu:latest
    steps:
      - run:
          echo "abc" > '/tmp/a b c'
      - save_cache:
          key: foo
          paths:
            - /tmp
