version: 2
jobs:
  build:
    docker:
      - image: ubuntu:latest
    steps:
      - checkout
      - echo "foo!"

workflows:
  version: 2
  workflow:
    jobs:
      - build:
          pre-steps:
            - run: "echo 'before checkout'"
